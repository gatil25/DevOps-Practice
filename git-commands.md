Setup & Config
- git init (Initialize Git Repo)
- git config --global user.name (Add global Username)
- git config --global user.email (Add global email)
- git config --list (verify config)

Basic Workflow
- 




Viewing Changes
